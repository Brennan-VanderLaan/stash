fastapi
uvicorn[standard]
python-multipart
jinja2
anthropic
python-dotenv
pydantic
qrcode[pil]
google-genai
Pillow
markdown
# Cricut/print-friendly label exports go through cairosvg → vector PDF and
# pypdf to merge per-sheet PDFs into one multi-page download.
cairosvg
pypdf
# Localization seams — gettext-driven message catalog + Babel for date /
# number / currency formatting.  v1 ships English-only but every user-
# visible string gets wrapped now so future translations are a
# translation task, not an engineering one.  See spec § "Localization".
Babel
# AES-256-GCM for at-rest encryption of photos + thumbs.  Per-tenant DEK
# wrapped by a STASH_KEK env var; see spec § "Encryption at rest" and
# vault.py for the key-handling story.
cryptography
# S3-compatible client for the B2 nightly DR target.  Spec § "Backups ·
# Off-site DR via Backblaze B2" picks B2 over S3 for cost and the spec
# explicitly notes the SDK choice: S3-compatible API via boto3 to avoid
# a bespoke B2 SDK.  Lives behind a factory in dao/backups so a unit
# test can swap in a stub without hitting the real network.
boto3

# Stripe SDK for billing.  Pro tier = 'bigger quotas only' today
# (see _PLAN_DEFAULTS in dao/quotas.py); the integration is a thin
# Checkout-session + webhook flow that flips tenants.plan from
# 'free' to 'pro' on subscription events.  Stash refuses-to-show-
# upgrade-CTA cleanly when STRIPE_SECRET_KEY is unset, same pattern
# as the B2 surface.
stripe

# XLSX parsing for the /import flow.  Encircle's "Detailed
# Spreadsheet" export is .xlsx with embedded images + a separate
# media .zip — the importer reads the XLSX with openpyxl to get
# text fields and (later) images via ``xl/media/``.  Light dep
# (~5 MB), no system requirements.
openpyxl
